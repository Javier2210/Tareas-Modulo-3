#include<iostream>
using namespace std;

int main() {
    int numero, invertido;
    cout << "Ingrese un número de dos cifras: ";
    cin >> numero;
    invertido = (numero % 10) * 10 + (numero / 10);
    cout << "El numero invertido es: " << invertido << endl;
    return 0;
}
